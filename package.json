{
  "name": "magic-sort-astro",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "build:game": "node scripts/build-game-bundle.mjs",
    "test:telemetry": "node scripts/test-game-telemetry.mjs",
    "validate:content": "node scripts/validate-question-articles.mjs",
    "predev": "npm run build:game",
    "dev": "astro dev",
    "prebuild": "npm run build:game",
    "build": "npm run test:telemetry && npm run validate:content && astro check && astro build && node scripts/cleanup-dist-seo.mjs && node scripts/audit-built-seo.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/sitemap": "3.5.1",
    "@tailwindcss/vite": "^4.3.0",
    "astro": "^5.13.5",
    "tailwindcss": "^4.3.0"
  },
  "devDependencies": {
    "@astrojs/check": "latest",
    "typescript": "latest"
  }
}
